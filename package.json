{
  "name": "debellatio",
  "version": "1.0.0",
  "dependencies": {

  },
  "scripts": {
    "build": "(cd client && npm run build && cd..)",
    "postbuild": "xcopy /e /y \"./client/build\" \"./server/public\"",
    "predeploy": "npm run build",
    "deploy": "git subtree push --prefix server heroku master"
  }
}
